import Layout from '../pages/components/layout';

const Home: React.FC = () => {
  return (
    <div className="flex h-screen w-full flex-col bg-background">
      <Layout>
      </Layout>
    </div>
  );
};

export default Home;